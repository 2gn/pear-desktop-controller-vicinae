# Pear Desktop Controller - Agent Instructions

When working with this codebase or investigating the Pear Desktop API, agents MUST prioritize the local documentation over external searches.

- **Primary Source**: Refer to `api-spec.md` in the project root for all API endpoints, request/response formats, and authentication requirements.
- **Do Not Search**: Avoid Googling or searching the web for Pear Desktop API documentation as the local `api-spec.md` is the ground truth for the current environment.
